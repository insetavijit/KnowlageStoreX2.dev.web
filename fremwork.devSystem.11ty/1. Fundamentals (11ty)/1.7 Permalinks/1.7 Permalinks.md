| Topic | Focus & Purpose |
| --- | --- |
| **[[1.7.1 Permalink Basics]]** | URL control fundamentals: setting custom output paths via `permalink`; simple string mapping; preventing file output with `permalink: false`; relationship between input path and output path. URL structure. |
| **[[1.7.2 Variable Interpolation]]** | Dynamic URLs: using template syntax in permalinks (e.g., `/blog/{{ title \| slugify }}/`); accessing `page` data; using filters like `slugify` and `lower`; escaping special characters. Dynamic URLs. |
| **[[1.7.3 Date-Based Permalinks]]** | Chronological structure: building date-based URLs (`/2024/01/15/post-title/`); using `page.date` with Liquid/Nunjucks date filters; custom date formats; year/month archive patterns. Chronological organization. |
| **[[1.7.4 Pagination & Permalinks]]** | Multi-page routing: configuring permalinks for paginated content; `pagination.href.first`, `pagination.href.next`; page number insertion (`/page-{{ pagination.pageNumber }}/`); avoiding collisions. Multi-page routing. |
| **[[1.7.5 Clean URLs vs File Extensions]]** | URL aesthetics: outputting `/about/index.html` (clean URL `/about/`) vs `/about.html`; configuring trailing slashes; impact on SEO and user experience; server configuration considerations. SEO & Aesthetics. |
| **[[1.7.6 Programmatic Permalinks]]** | Code-driven URLs: using JavaScript functions in `eleventyComputed.permalink`; conditional permalinks based on data; draft/publish workflows; generating sitemaps dynamically. Advanced URL logic. |
