|Topic|Focus & Purpose|
|---|---|
|**[[1.3.1 Input & Output Directories]]**|Configuring `dir.input` and `dir.output`; separating source code from compiled site; best practices for folder organization. Core paths.|
|**[[1.3.2 The `_includes` Directory]]**|Storing layouts, partials, and components; understanding the special `_includes` resolution behavior; organizing reusable code. Reusability hub.|
|**[[1.3.3 The `_data` Directory]]**|Global data files (JSON, YAML, JS); cascading data; directory-specific data files; fetching external data. Data layer.|
|**[[1.3.4 Passthrough File Copy]]**|Handling static assets (CSS, images, fonts); `addPassthroughCopy`; copying files vs folders; preserving directory structure. Asset management.|
|**[[1.3.5 Template & Content Organization]]**|Structuring content collections (e.g., `src/posts`, `src/pages`); grouping related templates; managing large sites. Content architecture.|
