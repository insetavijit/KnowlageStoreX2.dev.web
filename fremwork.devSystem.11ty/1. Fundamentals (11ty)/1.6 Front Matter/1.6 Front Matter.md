| Topic | Focus & Purpose |
| --- | --- |
| **[[1.6.1 YAML Front Matter Basics]]** | Syntax fundamentals: YAML structure between `---` delimiters; valid data types (strings, numbers, booleans, arrays, objects); indentation rules; multiline strings with `\|` and `>`. Metadata foundation. |
| **[[1.6.2 Special Keys]]** | Reserved page properties: `layout` for template wrapping; `title` for page titles; `permalink` for URL control; `tags` for collections; `date` for sorting; `eleventyExcludeFromCollections`; `eleventyNavigation`. Core properties. |
| **[[1.6.3 Computed Data]]** | Dynamic metadata: `eleventyComputed` for generating values from other data; dependency resolution order; accessing `page` and `collections`; async computed functions; practical use cases (slugs, dates). Dynamic metadata. |
| **[[1.6.4 Custom Data & JSON Front Matter]]** | Data flexibility: defining arbitrary page variables; using JSON syntax (`;;;`) as alternative to YAML; JavaScript front matter with `---js`; accessing custom data in templates. Data flexibility. |
| **[[1.6.5 Data Cascade Priority]]** | Inheritance rules: how front matter overrides directory data (`*.11tydata.js`), global data (`_data`), and layout data; merge vs override behavior; debugging data conflicts. Override hierarchy. |
| **[[1.6.6 Front Matter in Layouts]]** | Layout-level defaults: setting shared metadata in layout files; inheritance through layout chains; when to use layout front matter vs page front matter. Default values. |
