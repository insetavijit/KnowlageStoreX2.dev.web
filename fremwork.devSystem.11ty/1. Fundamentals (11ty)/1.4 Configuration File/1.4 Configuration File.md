|Topic|Focus & Purpose|
|---|---|
|**[[1.4.1 The `.eleventy.js` File]]**|The heart of an Eleventy project; basic export structure; passing configuration arguments. Config entry point.|
|**[[1.4.2 Configuration API]]**|Common methods: `addCollection`, `addFilter`, `addShortcode`, `addPlugin`; customizing the build process. API overview.|
|**[[1.4.3 Return Object & Options]]**|Configuring input/output directories, template engine defaults, and path prefixes via the return object. Global settings.|
|**[[1.4.4 Async Configuration]]**|Using async functions in configuration; fetching remote data during config; awaiting plugins. Modern config patterns.|
|**[[1.4.5 Configuration Events]]**|Hooking into build events (`eleventy.before`, `eleventy.after`); running custom scripts; logging and debugging. Build lifecycle.|
