| **Subtopic** | **Focus & Purpose** | **Key Concepts / Details** | **One-Line Recall** |
| :--- | :--- | :--- | :--- |
| **1.4.5 Configuration Events** | Hooking into the lifecycle | `eleventy.before`, `eleventy.after`, `eleventy.contentMap`; running scripts | Triggers that fire during the build. |
| **[[1.4.5.1 The Event Lifecycle]]** | Timeline | Order of events; when configuration loads vs when build starts; watch mode cycles | Know when things happen. |
| **[[1.4.5.2 eleventy.before]]** | Pre-build hook | Running tasks *before* files are processed; cleaning folders; fetching data | Do this before you start building. |
| **[[1.4.5.3 eleventy.after]]** | Post-build hook | Running tasks *after* the build; generating search indexes; notifying services | Do this after the site is ready. |
| **[[1.4.5.4 Argument Data]]** | Event context | Accessing `dir`, `outputMode`, `runMode` in event handlers; knowing the build state | Events provide context about the build. |
| **[[1.4.5.5 Practical Uses]]** | Examples | CSS processing (LightningCSS/PostCSS); logging stats; error reporting | Real-world reasons to hook into events. |
