| **Subtopic** | **Focus & Purpose** | **Key Concepts / Details** | **One-Line Recall** |
| :--- | :--- | :--- | :--- |
| **1.4.4 Async Configuration** | Modern Config Patterns | `module.exports = async function()`; awaiting promises; API calls | Configuration that can wait for data. |
| **[[1.4.4.1 Why Async?]]** | Use Cases | Fetching remote data before build; awaiting slow plugins; database connections | Sometimes setup takes time. |
| **[[1.4.4.2 Syntax Conversion]]** | Implementation | Changing sync function to `async`; using `await`; error handling | Adding `async` keyword unlocks promises. |
| **[[1.4.4.3 Awaiting Plugins]]** | Plugin compatibility | `await this.addPlugin()`; when it matters (file system operations); sync vs async plugins | Loading extensions that need time to prep. |
| **[[1.4.4.4 Fetching API Data]]** | config-time data | Calling fetch/axios inside config (rare but possible); populating global data dynamically | Getting external data before the build starts. |
| **[[1.4.4.5 Limitations]]** | Caveats | Build time impact; complexity; debugging async failures | Async config adds power but complexity. |
