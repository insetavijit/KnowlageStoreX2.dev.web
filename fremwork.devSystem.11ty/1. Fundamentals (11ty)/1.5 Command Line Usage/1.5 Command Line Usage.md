| Topic | Focus & Purpose |
| --- | --- |
| **[[1.5.1 Basic Commands]]** | Core CLI usage: `npx eleventy` for building; `--version` to verify installation; `--help` for command reference; understanding default behavior and exit codes. CLI foundation. |
| **[[1.5.2 Serving & Watching]]** | Development workflow: `--serve` for local server with live reload (Browsersync/Dev Server); `--watch` for file monitoring without server; `--port` customization; `--quiet` for minimal output. Local development. |
| **[[1.5.3 Debugging & Dry Runs]]** | Diagnostics toolkit: `--dryrun` to simulate builds without file writes; `DEBUG=Eleventy*` for verbose logging; `--to=json` for data inspection; troubleshooting silent failures and template errors. Problem solving. |
| **[[1.5.4 Advanced Flags]]** | Runtime overrides: `--input` and `--output` to override directories; `--config` to specify alternate config file; `--formats` to filter template types; `--ignore-initial` for watch mode optimization. CLI power-user options. |
| **[[1.5.5 Incremental Builds]]** | Performance optimization: `--incremental` for partial rebuilds; dependency tracking; cache invalidation logic; when incremental helps vs hurts; combining with watch mode. Build speed. |
| **[[1.5.6 Environment Variables]]** | Build context: `ELEVENTY_RUN_MODE` (`build`, `watch`, `serve`); `ELEVENTY_ROOT`; custom env vars for conditional logic; `.env` file usage with `dotenv`; production vs development builds. Contextual configuration. |
