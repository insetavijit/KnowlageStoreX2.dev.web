| **Subtopic** | **Focus & Purpose** | **Key Concepts / Details** | **One-Line Recall** |
| :--- | :--- | :--- | :--- |
| **1.2.1 HTML & Markdown Basics** | Core content languages | Pass-through vs. processed; parsing; extensibility | The foundation of static content. |
| **[[1.2.1.1 Markdown Processing]]** | Writing content | CommonMark compliance; library choice (`markdown-it`); overriding defaults | Markdown is the standard for text content. |
| **[[1.2.1.2 HTML Passthrough]]** | Using raw HTML | `.html` files in input; when to use raw HTML over templates; performance | Valid HTML is valid 11ty content. |
| **[[1.2.1.3 Markdown Plugins]]** | Extending Markdown | Adding features to parser (footnotes, anchors); configuration in `.eleventy.js` | Plugins give Markdown superpowers. |
| **[[1.2.1.4 Pre-processing vs Post]]** | Render order | Markdown processed to HTML; filters applied after; shortcodes within Markdown | Understand how text becomes HTML. |
| **[[1.2.1.5 Permalinks & Keys]]** | File-specific settings | Using front matter to set URLs; standard data keys | key-value pairs control the page output. |
