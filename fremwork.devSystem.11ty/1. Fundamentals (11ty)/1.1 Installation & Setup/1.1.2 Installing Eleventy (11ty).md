| **Subtopic** | **Focus & Purpose** | **Key Concepts / Details** | **One-Line Recall** |
| :--- | :--- | :--- | :--- |
| **1.1.2 Installing Eleventy (11ty)** | Acquire the core tool for the project | npm install, devDependencies, local scope | Installing 11ty locally ensures project stability. |
| **[[1.1.2.1 The Install Command]]** | Install the package correctly | `npm install @11ty/eleventy --save-dev`; `package.json` entry; `node_modules` | Use `--save-dev` to mark it as a tool, not a site asset. |
| **[[1.1.2.2 Local vs Global]]** | Understand scope isolation | Avoiding `npm install -g`; preventing version conflicts; CI/CD compatibility | Always install 11ty locally to lock versions per project. |
| **[[1.1.2.3 Running Eleventy]]** | Execute the local binary | `npx @11ty/eleventy`; `npx eleventy` alias; accessing the CLI | `npx` executes the local version of 11ty. |
| **[[1.1.2.4 Release Channels]]** | Manage versions and updates | Stable releases; Canary (beta) builds; semver ranges (`^2.0.0`) | Choose Stable for production, Canary for new features. |
| **[[1.1.2.5 Post-Install Check]]** | Verify successful installation | Checking `package.json`; verifying `node_modules`; running `--version` | Confirm installation by checking the version output. |
