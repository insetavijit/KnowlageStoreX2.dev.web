| **Subtopic** | **Focus & Purpose** | **Key Concepts / Details** | **One-Line Recall** |
| :--- | :--- | :--- | :--- |
| **1.1.7 Development Scripts** | Automating workflows | `package.json` scripts; build vs. serve; workflow efficiency | Scripts save you from typing long commands. |
| **[[1.1.7.1 Defining npm Scripts]]** | Standardizing commands | `scripts` object in `package.json`; running via `npm run`; command abstraction | Naming detailed commands for easy reuse. |
| **[[1.1.7.2 The Build Script]]** | Production output | `eleventy` command; clean builds; file optimization (future); CI/CD usage | "build" creates the final site for deployment. |
| **[[1.1.7.3 The Serve Script]]** | Local development | `eleventy --serve`; combining watch and serve; local server settings | "start" or "dev" runs your local lab. |
| **[[1.1.7.4 Watch Mode]]** | Background compilation | `eleventy --watch`; rebuilding without serving; file monitoring | Watch mode keeps files fresh without a server. |
| **[[1.1.7.5 Debugging Scripts]]** | Troubleshooting | `DEBUG` flag; verbose output; diagnosing build errors | Debug scripts reveal what's happening under the hood. |
