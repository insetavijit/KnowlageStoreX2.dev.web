| Topic                                     | Focus & Purpose                                                                                                                                                       |
| ----------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **[[1.1.1 Node.js & Environment Setup]]** | Node.js LTS requirements; npm vs npx; version verification; global vs local installs; OS considerations (Windows/Linux/macOS). Runtime foundation.                    |
| **[[1.1.2 Installing Eleventy (11ty)]]**  | Installing 11ty via `npm install @11ty/eleventy`; local project dependency; using `npx eleventy`; checking installation; common install errors. Tool acquisition.     |
| **[[1.1.3 Project Initialization]]**      | Initializing a new project; `package.json` creation; basic npm scripts; first `eleventy` run; understanding zero-config behavior. Project bootstrap.                  |
| **[[1.1.4 Default Directory Structure]]** | Input/output directories (`src`, `_site`); includes and layouts; data folders; static assets; understanding Eleventy’s file resolution rules. Structural layout.      |
| **[[1.1.5 First Eleventy Site]]**         | Creating the first HTML/Markdown file; front matter basics; building and serving the site; verifying output; hot reload with `--serve`. First build cycle.            |
| **[[1.1.6 Zero-Config Start]]**           | Eleventy defaults; conventions over configuration; supported template types; implicit behaviors; when configuration is optional vs required. Beginner-friendly setup. |
| **[[1.1.7 Development Scripts]]**         | npm scripts for build and serve; watch mode; clean builds; environment separation (dev vs prod); command standardization. Workflow readiness.                         |
| **[[1.1.8 Common Setup Pitfalls]]**       | Node version mismatches; missing dependencies; path issues; Windows-specific quirks; permission errors; troubleshooting startup failures. Setup stability.            |