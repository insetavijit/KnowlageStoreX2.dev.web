| **Subtopic** | **Focus & Purpose** | **Key Concepts / Details** | **One-Line Recall** |
| :--- | :--- | :--- | :--- |
| **1.1.3 Project Initialization** | Structuring the project foundation | `package.json`, scripts, first run | Setting up the project manifest and run scripts. |
| **[[1.1.3.1 npm init & package.json]]** | Creating the project manifest | `npm init -y`; understanding fields (`name`, `scripts`, `devDependencies`); version control | `npm init` generates the `package.json` file. |
| **[[1.1.3.2 Adding npm Scripts]]** | Simplifying command execution | `"build": "eleventy"`, `"serve": "eleventy --serve"`; why scripts are better than raw `npx` | npm scripts alias complex commands to simple keywords. |
| **[[1.1.3.3 The First Build]]** | Executing the first compilation | Running `npm run build`; observing the `_site` output folder creation | The first build proves the environment is ready. |
| **[[1.1.3.4 Zero-Config Defaults]]** | Understanding implicit behavior | Default input (`.`), default output (`_site`); markdown processing; liquid/njk support | 11ty processes files without any config file needed. |
| **[[1.1.3.5 Ignoring Files (.gitignore)]]** | Managing version control | Excluding `node_modules` and `_site` from git; keeping the repo clean | Always ignore generated files and dependencies. |
