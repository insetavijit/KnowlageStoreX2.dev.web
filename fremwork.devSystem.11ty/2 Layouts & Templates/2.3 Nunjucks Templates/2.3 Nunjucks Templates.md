| Topic | Focus & Purpose |
| --- | --- |
| **[[2.3.1 Variables & Types]]** | Data structures: declaring variables with `{% set %}`; primitive types (strings, numbers, booleans); complex types (arrays, objects); scoping rules; null/undefined handling; truthy/falsy values. Data foundations. |
| **[[2.3.2 Filters & Pipe Syntax]]** | Data transformation: using the pipe operator `\|`; chaining multiple filters; built-in Nunjucks filters (`safe`, `lower`, `upper`, `join`, `first`, `last`); defining custom filters in `.eleventy.js`. Output modification. |
| **[[2.3.3 Control Structures]]** | Logic flow: conditional rendering with `if`/`elif`/`else`/`endif`; `for` loops with `for...in`; loop variables (`loop.index`, `loop.first`, `loop.last`, `loop.length`); `break` and `continue`; async loop handling. Branching and iteration. |
| **[[2.3.4 Macros (Reusable Functions)]]** | Component logic: defining macros with `{% macro name(args) %}`; default argument values; calling macros; importing macros from other files with `{% from %}` and `{% import %}`; use cases for buttons, cards, and UI patterns. Reusable components. |
| **[[2.3.5 Includes & Imports]]** | Modular code: `{% include %}` for simple file inclusion; `{% import %}` for namespaced macros; `{% from %}` for selective imports; path resolution; passing context to includes; error handling for missing files. Code organization. |
| **[[2.3.6 Nunjucks Environment]]** | Advanced configuration: configuring the Nunjucks environment in `.eleventy.js`; `setNunjucksEnvironmentOptions`; autoescaping settings; custom global functions; adding extensions; performance tuning. Engine customization. |
