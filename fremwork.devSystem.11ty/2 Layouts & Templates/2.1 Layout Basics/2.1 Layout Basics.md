
| Topic                                        | Focus & Purpose                                                                                                                                                                                                                       |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **[[2.1.1 Creating Layouts]]**               | Foundation: understanding the `_includes` directory; creating a basic layout file (e.g., `base.njk`); wrapper templates; the `{{ content }}` variable for injecting page content; DOCTYPE and HTML boilerplate. Structure foundation. |
| **[[2.1.2 Layout Aliasing]]**                | Configuration convenience: mapping shorthand aliases to layout file paths in `.eleventy.js`; `eleventyConfig.addLayoutAlias()`; simplifying front matter; renaming layouts without breaking content. Clean references.                |
| **[[2.1.3 Directory Specific Layouts]]**     | Scoped defaults: setting layouts via directory data files (`directory.11tydata.js`); applying layouts to all files in a folder; cascading behavior; overriding at the page level. Folder-wide settings.                               |
| **[[2.1.4 Layout Chaining]]**                | Modular design: nesting layouts where a layout uses another layout; building a hierarchy (e.g., `base` → `page` → `post`); passing data up the chain; avoiding duplication. Hierarchical structure.                                   |
| **[[2.1.5 Front Matter Layout Assignment]]** | Direct control: specifying `layout: layoutName` in front matter; overriding directory defaults; using `layout: false` to bypass layouts entirely; conditional layout selection. Page-level override.                                  |
| **[[2.1.6 Layout Best Practices]]**          | Quality patterns: naming conventions; keeping layouts DRY; separating concerns (structure vs content); when to split layouts; avoiding deep nesting; commenting layout files. Maintainability.                                        |

Review dates 
05012026 - 