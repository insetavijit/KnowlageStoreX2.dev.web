| **Subtopic** | **Focus & Purpose** | **Key Concepts / Details** | **One-Line Recall** |
| :--- | :--- | :--- | :--- |
| **2.8.5 Async Rendering** | Data Fetching | `async render()`; `await`; Promises; API calls | Queries inside the template. |
| **[[2.8.5.1 Async Functions]]** | Syntax | `async render(data) { ... }`; enables `await` keyword | Unblocking the main thread. |
| **[[2.8.5.2 Fetching Data]]** | Usage | `const res = await fetch(url)`; calling APIs during build | Get fresh data now. |
| **[[2.8.5.3 Promise.all]]** | Performance | `await Promise.all([p1, p2])`; running parallel requests | Fetching multiple things fast. |
| **[[2.8.5.4 Error Handling]]** | Robustness | `try { ... } catch (e) { ... }`; preventing build failures | Don't crash on 404. |
| **[[2.8.5.5 Comparison]]** | Context | vs Global Data Files (fetching before build); Fetching *during* render (per page) | Fetch when you render. |
