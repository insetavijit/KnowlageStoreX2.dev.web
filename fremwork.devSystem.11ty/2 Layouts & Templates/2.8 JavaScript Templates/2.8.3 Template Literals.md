| **Subtopic** | **Focus & Purpose** | **Key Concepts / Details** | **One-Line Recall** |
| :--- | :--- | :--- | :--- |
| **2.8.3 Template Literals** | Native JS Templating | Backticks; interpolation `${}`; tagging | JS strings on steroids. |
| **[[2.8.3.1 Backtick Syntax]]** | Basics | Multi-line strings support; no more `+` concatenation | Strings that span lines. |
| **[[2.8.3.2 Interpolation]]** | Logic | `${data.title}`; evaluating expressions inside strings | Injecting data. |
| **[[2.8.3.3 Conditionals]]** | Logic | Using ternaries `${cond ? 'a' : 'b'}` or function calls | Logic inside the string. |
| **[[2.8.3.4 Loops (Map)]]** | Iteration | `${arr.map(item => `<li>${item}</li>`).join('')}`; Array mapping | Looping with .map(). |
| **[[2.8.3.5 Tagged Templates]]** | Advanced | `html`Hello ${world}``; parsing template literals with functions (e.g., lit-html) | Custom string processing. |
