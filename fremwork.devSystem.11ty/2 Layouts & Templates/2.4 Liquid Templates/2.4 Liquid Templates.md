| Topic | Focus & Purpose |
| --- | --- |
| **[[2.4.1 Objects & Tags]]** | Core syntax: output markup `{{ variable }}`; tag markup `{% tag %}`; comment syntax `{% comment %}`; whitespace control with `{%-` and `-%}`; understanding Liquid's strict syntax constraints. Syntax fundamentals. |
| **[[2.4.2 Standard Filters]]** | String & array manipulation: common filters (`date`, `capitalize`, `downcase`, `upcase`, `append`, `prepend`, `split`, `join`, `first`, `last`, `size`, `reverse`); number formatting; array mapping. Data transformation. |
| **[[2.4.3 Iteration & Logic]]** | Flow control: `{% for item in array %}` loops; `limit` and `offset` parameters; `reversed` modifier; `{% tablerow %}` for table generation; `{% if %}`, `{% unless %}`, `{% elsif %}`, `{% case %}`/`{% when %}`; operators. Branching and looping. |
| **[[2.4.4 Assignments & Capture]]** | Variable management: `{% assign variable = value %}`; creating variables from expressions; `{% capture %}` blocks for building complex strings; increment/decrement counters; variable scope. Data storage. |
| **[[2.4.5 Jekyll Compatibility]]** | Migration path: shared syntax heritage with Jekyll; differences in filter availability; plugin ecosystem gaps; migrating Jekyll themes to 11ty; leveraging existing Liquid knowledge. Jekyll migration. |
| **[[2.4.6 Liquid Mode Configuration]]** | Engine settings: `liquidOptions` in `.eleventy.js`; strict vs lenient mode; custom tags and filters; `extname` settings; dynamic partials; trusted templates configuration. Engine customization. |
