|**Subtopic**|**Focus & Purpose**|**Key Concepts / Details**|**One-Line Recall**|
|---|---|---|---|
|**[[4.6.1 Static Search Index]]**|Generate searchable content index at build|Index creation, JSON generation|A search index makes static site content searchable.|
|**[[4.6.1.1 Index Data Structure]]**|Design the search index format|JSON schema, fields to index|Define what fields the search index contains.|
|**[[4.6.1.2 Content Extraction]]**|Pull searchable content from pages|Title, URL, tags, body text|Extract the content that users want to search.|
|**[[4.6.1.3 Index Template]]**|Create the index-generating template|`search.json.njk`, collection iteration|A template generates the JSON search index.|
|**[[4.6.1.4 HTML Stripping]]**|Remove markup from indexed content|`striptags` filter, plain text extraction|Strip HTML to index clean text content.|
|**[[4.6.1.5 Index Optimization]]**|Reduce index size for performance|Field selection, truncation, compression|Smaller indexes load and search faster.|
|**[[4.6.1.6 Incremental Indexing]]**|Update only changed content|Partial indexes, build optimization|Incremental indexing speeds up large site builds.|
