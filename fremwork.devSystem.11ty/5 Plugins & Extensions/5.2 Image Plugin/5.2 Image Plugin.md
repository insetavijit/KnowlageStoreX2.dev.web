|Topic|Focus & Purpose|
|---|---|
|**[[5.2.1 Image Optimization]]**|Resizing, format conversion (WebP/AVIF); generating multiple sizes; `eleventy-img` mechanics. Core features.|
|**[[5.2.2 Async Shortcodes]]**|Using async shortcodes for image processing; preventing build blocking; `{% image %}` usage. Template integration.|
|**[[5.2.3 HTML Attributes]]**|Generating `picture`, `source`, and `img` tags; `loading="lazy"`; `decoding="async"`; alt text. Markup generation.|
|**[[5.2.4 Caching Images]]**|Where images are stored; caching processed images; preventing re-processing on every build. Performance.|
|**[[5.2.5 Background Images]]**|Generating CSS background images via the image plugin; direct URL access. Advanced usage.|
