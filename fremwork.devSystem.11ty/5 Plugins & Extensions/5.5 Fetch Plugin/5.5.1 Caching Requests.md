|**Subtopic**|**Focus & Purpose**|**Key Concepts / Details**|**One-Line Recall**|
|---|---|---|---|
|**[[5.5.1 Caching Requests]]**|Cache fetched data for efficiency|Efficiency, API rate limits|Caching prevents redundant API calls.|
|**[[5.5.1.1 Cache Duration]]**|Set how long cache is valid|Duration strings, `1d`, `4h`|Duration controls when cached data expires.|
|**[[5.5.1.2 Cache Keys]]**|Understand cache identification|URL-based keys, uniqueness|Each unique URL gets its own cache entry.|
|**[[5.5.1.3 Cache Location]]**|Know where cache is stored|`.cache` directory, file system|Fetch plugin stores cache in `.cache` directory.|
|**[[5.5.1.4 Cache Invalidation]]**|Force fresh data when needed|Manual clear, duration expiry|Invalidate cache when source data changes.|
|**[[5.5.1.5 CI/CD Cache Persistence]]**|Keep cache across deployments|Build caching, cache restoration|Persist cache in CI for faster builds.|
|**[[5.5.1.6 Offline Support]]**|Use cache when offline|Stale cache, build resilience|Cache enables builds when APIs are unavailable.|
