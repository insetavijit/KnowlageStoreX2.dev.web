|Topic|Focus & Purpose|
|---|---|
|**[[3.1.1 Tag-Based Collections]]**|Creating collections by adding `tags` to front matter; `collections.tagname`; iterating over collections. Simplest grouping.|
|**[[3.1.2 The `all` Collection]]**|`collections.all`; iterating over every page in the site; excluding content (`eleventyExcludeFromCollections`). Global access.|
|**[[3.1.3 Custom API Collections]]**|Using `eleventy.addCollection`; creating collections programmatically from data; advanced grouping logic. Custom structures.|
|**[[3.1.4 Collection Items]]**|Understanding the collection item object; `url`, `date`, `data`, `templateContent`; accessing page metadata. Item anatomy.|
|**[[3.1.5 Collection Debugging]]**|Logging collections to console; inspecting collection structure during build. Troubleshooting.|
