|**Subtopic**|**Focus & Purpose**|**Key Concepts / Details**|**One-Line Recall**|
|---|---|---|---|
|**1.2.2 The HTML Element**|Define the root element of the document|Root node, global attributes (lang), parent of head/body|The `<html>` element is the root container for everything in the document.|
|**[[1.2.2.1 The Root Node]]**|Structural position|Top of the DOM tree, contains all other elements|Every valid HTML document has exactly one root element.|
|**[[1.2.2.2 The Lang Attribute]]**|Accessibility and SEO criticals|`lang="en"`, ISO language codes, screen reader pronunciation|The `lang` attribute tells assistive tech how to read the content.|
|**[[1.2.2.3 Permitted Content]]**|What goes inside|Exactly one `<head>` followed by exactly one `<body>`|The html element has strict rules: one head, then one body.|
|**[[1.2.2.4 XML Namespace (Optional)]]**|XHTML legacy|`xmlns` attribute, use in polyglot markup|Usually unnecessary in HTML5, but valid for compatibility.|
|**[[1.2.2.5 Browser Defaults]]**|How it behaves|`display: block`, relationship to viewport height|Browsers give the root element default display properties.|
|**[[1.2.2.6 Semantic Value]]**|Meaning of the root|Defining the scope of the web document|It delimits where the document begins and ends.|
